<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mid.mapper.mainMapper">
	
	<select id="userCount" resultType="Integer">
		SELECT COUNT(*) FROM mentoruser UNION SELECT COUNT(*) FROM mentiuser;
	</select>

	<select id="getboardnumList" resultType="Integer">
		SELECT num FROM mentiBoard;
	</select>
	
	<select id="getUserList" resultType="com.mid.VO.userVO">
		SELECT id, num, IFNULL(city, 1)city, IFNULL(profile_image, 1)profile_image FROM mentiuser
			UNION ALL SELECT id, num, IFNULL(city, 1)city, IFNULL(profile_image, 1)profile_image FROM mentoruser;
	</select>
	
	<select id="getCityCoordinates" resultType="com.mid.VO.cityCoordinatesVO">
		SELECT * FROM citiescoordiates;
	</select>
	
	<select id="userPointRank" resultType="com.mid.VO.userPointVO">
		SELECT * FROM mentoruser mu JOIN (SELECT mentorNum, sum(earnAmount)earnAmount FROM mentorpoint GROUP BY mentorNum)t
			ON mu.num = t.mentorNum;
	</select>
	
	
</mapper>